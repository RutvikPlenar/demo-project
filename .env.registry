# Environment variables for Docker Registry
# Update these values with your actual registry information

# Your private Docker registry URL (without http://)
# Examples:
#   - registry.example.com:5000
#   - 192.168.1.101:32770
#   - plenar-nuc1.v6.rocks:5000
REGISTRY_URL=your-registry.example.com:5000

# Image name and tag
IMAGE_NAME=demo-project
IMAGE_TAG=latest

# Full image path (constructed from above)
FULL_IMAGE=${REGISTRY_URL}/${IMAGE_NAME}:${IMAGE_TAG}

# Optional: Registry authentication (if required)
# REGISTRY_USERNAME=your-username
# REGISTRY_PASSWORD=your-password
